LOKA_PORT=8181
GEMINI_API_KEY=replace-me

# --- LOKA Atlas wizard ---
# Admin auth for approvals + ops (any long random string)
ATLAS_ADMIN_TOKEN=replace-me
# Where approval requests are sent
ATLAS_ADMIN_EMAIL=mithun@socratus.org
# Public site base for links in emails (default: derived from request headers)
# ATLAS_PUBLIC_BASE=https://loka.place
# Free-tier thresholds and limits (defaults shown)
# ATLAS_FREE_MB=5
# ATLAS_MAX_AREA_DEG2=6      # regions above this need admin approval
# ATLAS_HARD_AREA_DEG2=40    # regions above this are refused outright
# ATLAS_MAX_INSTANCES=50
# ATLAS_PER_IP_PER_DAY=3
# Python for the dataset builder (default: api/atlas-builders/.venv/bin/python3)
# ATLAS_PYTHON=/usr/bin/python3

# --- outbound mail (magic links + approvals) ---
# Preference order: SMTP → local sendmail binary → pm2 log fallback.
# We send through Resend's SMTP interface; nothing here is Resend-specific
# beyond the values. Without SMTP_HOST the local sendmail binary is used, and
# mail from this host is very likely to be filtered — so set these in api/.env,
# which pm2 loads with --env-file.
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USER=resend
# SMTP_PASS=your-resend-api-key
# FROM_EMAIL=atlas@loka.place        # must be a domain verified in Resend
# MAIL_FROM="LOKA Atlas <atlas@loka.place>"

# --- local development only: serve the repo at /apps with the API rewrite ---
# LOKA_DEV_STATIC=1
